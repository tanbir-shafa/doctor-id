# Daktar.Link MVP — Progress

> **Plan:** [../plans/mvp-plan.md](../plans/mvp-plan.md)
> **Last updated:** 2026-06-06

Each entry mirrors a step in the plan's "Build Sequence" section. Update this file in the same commit that completes a step.

## Status legend
- `not-started` — no code written
- `in-progress` — branch open, partial work
- `blocked` — needs an external decision/credential before it can proceed
- `done` — merged + acceptance criteria met

## Tasks

| # | Task | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| 0 | Plan + progress artifacts checked in | done | 2026-05-28 | 2026-05-28 | This file + mvp-plan.md committed |
| 1 | Scaffold (Next.js pinned, Tailwind, shadcn, Vitest, Mongo conn, /api/health) | done | 2026-05-28 | 2026-05-28 | Next 16.2.6, React 19.2.4 pinned exact. /api/health returns 200 against Atlas. Homepage SSR + OG meta verified. |
| 2 | Models + seed script (20 specialties, 50 BD doctors, 1 admin) | done | 2026-05-28 | 2026-05-28 | All models with FHIR-aligned indexes. Seed inserted 20 specialties + 50 doctors + admin into Atlas. |
| 3 | Auth (NextAuth v5, SES verify, Upstash login rate-limit) | done | 2026-05-28 | 2026-05-28 | NextAuth v5 (Credentials + Google when configured) with JWT sessions. Edge-safe proxy.ts (renamed from middleware per Next 16). Verified end-to-end: admin credentials login returns valid session cookie, proxy guards /dashboard + /admin. SES sends are no-ops without AWS creds (logged instead). Upstash limiter no-ops without REST creds. |
| 4 | Public profile `/[slug]` — SSR, JSON-LD, OG, share/QR | done | 2026-05-28 | 2026-05-28 | SSR with `revalidate: 60`. Physician + MedicalBusiness JSON-LD verified in HTML source. Dynamic /api/og/[slug] returns valid 1200×630 PNG. Leaflet maps lazy-loaded client-side. View counter, share/QR, WhatsApp CTA, report button all wired. |
| 5 | Search + category pages + sitemap + robots | done | 2026-05-28 | 2026-05-28 | /search, /cardiology, /cardiology/dhaka all 200 with proper titles. sitemap.xml + robots.txt rendered. SpecialtyListing reused via polymorphic [slug]/page.tsx (specialty match wins over doctor match). |
| 6 | Doctor dashboard (profile editor, chambers, settings) | done | 2026-05-28 | 2026-05-28 | Dashboard layout, overview with completeness chart, profile editor (basic, contact, qualifications array, experience array, publish toggle), analytics (daily views chart, top referrers), chambers list (read-only — full editor deferred), settings with password change + soft-delete. |
| 7 | S3 photo + cover upload (presign + crop) | done | 2026-05-28 | 2026-05-28 | Presigned PUT URL flow (5MB cap, JPG/PNG/WebP, PDF for verification docs). Server confirms key→URL and writes to doctor.photo / .coverPhoto. Falls back gracefully when AWS creds missing. Client-side cropper (react-easy-crop) deferred to Step 10 polish. |
| 8 | Verification flow + admin panel | done | 2026-05-28 | 2026-05-28 | Doctor verification request page + admin /admin layout, /admin/verifications queue with approve/reject, /admin/doctors list with suspend, /admin/specialties read-only catalog. Role gate via proxy + layout guard. |
| 9 | Analytics + `/api/v1/*` (FHIR-shaped, rate-limited) | done | 2026-05-28 | 2026-05-28 | /api/v1/doctors, /[slug], /specialties, /search all 200. FHIR Practitioner shape verified. Upstash rate-limit returns 429 + Retry-After. CDN cache headers set. Dashboard analytics shows 30-day chart + top referrers. |
| 10 | Polish (a11y, Lighthouse, Dockerfile, README, ECS notes) | done | 2026-05-28 | 2026-05-28 | Multi-stage Dockerfile (Node 22 alpine, standalone, non-root, /api/health probe). docker-compose.yml local Mongo fallback. README rewritten with setup + AWS ECS deploy guide. Vitest suite: 26 tests in 6 files, all green. Production `next build` succeeds — 30 routes built. Lighthouse/axe pass deferred to post-merge in staging. |

## Blockers / heads-up

- **AWS SES** — sandbox by default. Production access request needs to be filed before public launch (~24h review). Tracked under Step 3.
- **AWS S3 bucket + IAM user** — need real credentials before Step 7 can be exercised end-to-end. Step 7 will be implementable but not testable without them.
- **AWS S3 re-platform (2026-06-03)** — uploads are now server-side with creds resolved by `NODE_ENV`: **production assumes a cross-account STS role** (`AWS_ASSUME_ROLE_ARN` + `AWS_S3_EXTERNAL_ID`, base creds from the EC2 instance profile / ECS task role via the default chain) and writes to a **public** + **private** bucket (`AWS_PUBLIC_BUCKET_NAME` / `AWS_PRIVATE_BUCKET_NAME`). Local dev uses static keys; the **private bucket has no fallback**, so selfie/verification uploads need it set locally too. Per decision, there is **no dev no-op** — registration genuinely requires working S3. See CLAUDE.md #17.
- **Upstash account** — need REST URL + token before Step 3 rate-limit + Step 9 API rate-limit can be exercised. Code falls back to a no-op limiter if env vars are missing so dev isn't blocked.
- **Google OAuth client** — need `AUTH_GOOGLE_ID` / `AUTH_GOOGLE_SECRET` before Google sign-in can be tested. Credentials sign-in works without it.
- **Atlas dev URI** — supplied 2026-05-28, in `.env.local`. Rotate before sharing with the team.

## Changelog

- **2026-05-28** — Plan approved, artifacts checked in, scaffolding started.
- **2026-05-28** — All 10 build-sequence steps completed in a single session. Production build green (30 routes), 26 Vitest tests passing, public API + JSON-LD + OG images verified against seeded data. Known deferrals: chambers editor UI, image cropper integration, Lighthouse/axe pass in staging.
- **2026-05-29** — Sprint A Day-0 fixes shipped (File model HMR-safe, PhotoSchema aligned, CLAUDE.md §12 added). Tests: 36 → 60 passing.
- **2026-05-29** — **A.1 Popular Diagnostic ingestion ✅** — `--source=popular-diagnostic` mode ships in `scripts/seed.ts`, with per-provider normalizer at `scripts/lib/providers/popular.ts`, alias map covering 30+ Popular labels (88% specialty match across 3,237 doctors), shared `phone.ts` + `name-parser.ts` utilities, server-side S3 upload helper, and `(sourceProvider, sourceProviderId)` idempotency. Smoke test: 10/10 inserted on first run, 10/10 updated on rerun. Pre-existing `bmdcNumber` sparse-index bug fixed via `partialFilterExpression`. Production build green, 60/60 tests passing.
- **2026-05-29** — **A.7 24h Verified Badge SLA ✅** — `slaExpiresAt` + `verifiedAt` on ClaimRequest with pre-validate/pre-save hooks; pure `src/lib/sla.ts` with `classifySla` + `formatDuration` (importable from client components without pulling mongoose); `listPendingClaimRequests` in new `src/lib/db/queries/admin.ts`; `/admin/verifications` shows bucket tiles + per-row SLA chip ordered by breach risk; `/dashboard/verification` advertises 24h promise with live countdown; OG image renders pill-style verification badge. 72/72 tests pass, production build green.
- **2026-05-29** — **A.6 Chambers Editor (full) ✅** — `ChambersUpdateSchema` validator with HH:mm regex, end-after-start, per-day overlap detection, max-10 cap, single-primary enforcement; `updateChambersAction` server action wholesale-replaces chambers atomically + recomputes completeness; `LeafletMap` extended with optional `onLocationChange` for click + drag-to-set pin; new `ScheduleEditor` (7-day grid with multi-slot per day) and `ChambersEditor` (RHF `useFieldArray`, map picker, schedule editor, primary mutex, add/remove). 82/82 tests pass, production build green.
- **2026-05-29** — **Bugfix: claim-by-public-CTA was silently dropped** — `/[slug]` CTA now passes `?slug=` to `/auth/register`. Register page looks up the target doctor server-side, blocks already-claimed slugs, prefills the form, and labels the submit button "Claim profile". `RegisterSchema` accepts an optional `claimSlug` and makes BMDC# optional when claiming by slug. `registerAction` has a new claim-by-slug branch that binds ownership atomically and creates a `pending` `ClaimRequest` for admin review (with 24h SLA per A.7). 85/85 tests pass.
- **2026-05-29** — **A.4 SMS Magic-Link Claim ✅** — MDL adapter (`src/lib/sms/client.ts`) with axios-shaped GET via native fetch, dev-mode console fallback, MDL-FAILED detection, ASCII/Unicode segment accounting. New `phone`, `phoneVerified`, `smsOtpHash/ExpiresAt/Attempts` fields on User with partial-filter unique index on phone. OTP utilities (`hashOtp`, 6-digit gen, 10-min TTL, 5-attempt cap). Rate-limiters: 3 OTP requests / 5 verify attempts per phone per 10min. Server actions: `requestSmsOtpAction` enforces phone-as-identity-proxy (silent no-op on phone mismatch to prevent leakage), `verifySmsOtpAndClaimAction` does atomic `isClaimed:false`-guarded claim + auto-creates pending ClaimRequest + signs in via NextAuth. NextAuth `sms-otp` Credentials provider added alongside email/password. Public `/[slug]` CTA leads with "Claim by SMS" + falls back to email. `/auth/claim?slug=...&phone=...` 2-step page (phone → OTP → redirect). Dashboard banner on `?welcome=1`. 98/98 tests pass.
- **2026-05-29** — **A.8 Bulk Outbound (SMS-only via MDL) ✅** — `OutboundMessage` + `OptOut` collections. New `sendSmsBatch` helper groups messages by identical body and chunks each group into ≤20 numbers per MDL API call (honoring the gateway's "20 numbers max + wait for success before next batch" rule). Identical-body broadcasts collapse 3,237 sends into ~165 calls; per-message personalization falls back to 1-per-call automatically. Two starter templates (`en-claim-rx-pad` / `bn-claim-rx-pad`) ship as broadcasts; `en-claim-rx-pad-personal` is opt-in. New `scripts/outbound.ts` (`npm run outbound -- --campaign=... --template=...`) does cohort filter → phone normalize → opt-out skip → 7-day idempotency dedupe → render → batch dispatch → per-row status persist. Claim attribution: `completeRegistrationAction` now stamps `OutboundMessage.claimedAt` for any sent row matching the new claimant's phone within 30 days. New `/admin/outbound` page shows per-campaign funnel (sent / failed / opted-out / claimed / CTR) and the opt-out roster with add/remove forms. Admin sidebar adds an Acquisition section. 148/148 tests pass; build green.
- **2026-05-29** — **A.5 Free Shafa EMR bundling (manual) ✅** — `User.emr` subdoc (`requested/seatStatus/accountEmail/readyAt/declinedAt`). Registration sets `pending` automatically. Admin server action `markEmrReadyAction` (admin-role-gated, Zod email validation, atomic `pending→ready` guard, idempotent on retries). Doctor `declineEmrAction` for opt-out. Dashboard banner with 3 states (pending/ready/hidden-on-declined). New `/admin/emr-queue` page sorted by claim age with inline "Mark ready" form per row + ExternalLink to public profile. Admin sidebar gets a new "EMR seats" entry with badge count. 132/132 tests pass; build green.
- **2026-05-29** — **A.3 Appointment Request Inbox ✅** — new `AppointmentRequest` model with status enum (pending/seen/booked/rejected), per-doctor + per-status indexes. Public `createAppointmentRequestAction` defense stack: honeypot field → IP rate-limit (3/h) → phone rate-limit (3/h) → Zod (30-day window, time-window enum, HTML-stripped reason) → atomic insert → fire-and-forget SMS to doctor's public phone. Doctor `updateAppointmentRequestStatusAction` is ownership-guarded via `loadMyDoctor()`. New `AppointmentRequestForm` (public sidebar slot on `/[slug]`, only for claimed+chambered doctors, snapshots chamber name at submit). Doctor inbox at `/dashboard/requests` with status filter chips + per-row actions (Mark seen, Mark booked, Reject, WhatsApp deep-link with prefilled message). Pending-count badge on the Requests nav entry — both desktop sidebar and mobile drawer pull a server-rendered count. 129/129 tests pass; build green.
- **2026-05-29** — **A.2 Rx Pad PDF generator ✅** — A5 prescription-pad PDF download at `/dashboard/prescription-pad/download` (auth-gated; admins blocked; doctor's own profile only). New deps `@react-pdf/renderer` + `qrcode`. Pure DTO builder `buildRxPadDto` returns either the DTO or a structured "missing fields" result. Server-side QR helper `renderQrPngDataUrl` (level H, photocopy-resilient). PDF component `RxPad` renders A5 portrait with photo / name / degrees / BMDC# / QR / chambers + schedule (day-range grouping like "Sat–Wed 5:00 PM – 9:00 PM") / signature line — **no medical content**. Preview iframe + download button at `/dashboard/prescription-pad`; missing-fields empty state deep-links to fix each gap. `Doctor.flags.rxPadGenerations` increments atomically per download; Sprint A KPI billboard reads from this. Nav entries added to desktop + mobile dashboard nav. 115/115 tests pass; build green; route handler verified at `/dashboard/prescription-pad/download`.
- **2026-05-29** — **Admin approval gate ✅** — every new doctor account is now created with `approved: false` and cannot log in until admin reviews the pending ClaimRequest in `/admin/verifications`. Approval flips both `Doctor.bmdcVerified` (when applicable) and `User.approved`. `requestLoginOtpAction` short-circuits with a "pending approval" message; `sms-otp` provider's `authorize` rejects unapproved users. Registration verify split out of `sms-otp` into its own `completeRegistrationAction` that materializes Doctor + ClaimRequest without signing in. `/admin` unauthed now bounces to `/auth/admin/login` (not the doctor phone form). Admin verification queue now surfaces requester phone/email/role/verified-status, document attachment count, "claim vs new signup" tag, and BMDC-mismatch hints. 102/102 tests still pass; production build green.
- **2026-05-29** — **Auth restructure ✅** — single auth model going forward:
  - **Doctors**: phone + SMS OTP only (no password). `/auth/login` is the 2-step phone form; `/auth/register` is unified for both new signups and slug-based claims, requires BMDC + phone + name + collapsible NID/selfie/supporting-doc uploads.
  - **Admins**: email + password at `/auth/admin/login`. Existing flow unchanged, just moved.
  - **regDraft subdoc on User** holds the in-flight registration payload between "OTP sent" and "OTP verified"; the NextAuth `sms-otp` provider's `authorize` callback materializes the Doctor + ClaimRequest atomically with the OTP check, so an abandoned registration leaves no half-bound profile.
  - `requestLoginOtpAction` for the login flow (silent no-op on unknown phones — no enumeration leak).
  - New `presignRegistrationDocAction` (unauthenticated, IP-rate-limited) lets the registration form upload NID/selfie/docs directly to S3 before submit; their s3Keys land on `ClaimRequest.documentsUploaded`.
  - Session cookie now carries explicit `maxAge: 30d` so the session survives browser restarts.
  - `/auth/claim` flow and `src/server/actions/claim.ts` removed — superseded by the unified flow. Public `/[slug]` CTA simplified to a single "Claim this profile" button → `/auth/register?slug=`.
  - Fixed pre-existing bug: `confirmProfilePhoto` was writing `{url, s3Key}` only; PhotoSchema now requires `s3Bucket` + `visibility` (since the Day-0 fix), so the action now sets them from env.
  - 100/100 tests pass; production build green; all 16 routes compile (added `/auth/admin/login`).
- **2026-06-03** — **S3 re-platform (port of `shafa-monorepo/apps/api`) ✅** — every upload moved off browser presigned-PUT to **server-side** `PutObject` (SSE-AES256) with authoritative `File`-doc persistence + replaced-object cleanup. New `src/lib/s3/{aws-credentials,s3-service,buckets,file-doc,doctor-photo,upload-doc}.ts`; `client.ts` resolves creds **by `NODE_ENV`** (prod → cross-account STS role via new dep `@aws-sdk/credential-providers`; else → static `.env` keys). **Two buckets**: public (profile/cover, stable region-qualified URLs) + private (selfie/verification, presigned-GET only). Migrated dashboard + admin profile/cover photo upload (shared `uploadDoctorPhotoFromForm`), verification-doc upload (now `File` refs on `ClaimRequest.documentFileIds` + presigned-GET in the admin queue — also fixing the pre-existing broken client-side `process.env.S3_BUCKET` link), and Popular ingestion. `next.config.ts` adds `experimental.serverActions.bodySizeLimit: "12mb"` + public/private bucket image hostnames. New env: `AWS_ASSUME_ROLE_ARN`, `AWS_S3_EXTERNAL_ID`, `AWS_PUBLIC_BUCKET_NAME`, `AWS_PRIVATE_BUCKET_NAME`, `MAX_FILE_SIZE_MB`, `MAX_FILES_COUNT`, `IMAGE_UPLOAD_PRESIGN_EXPIRES_SECONDS`. Dead `presign.ts`/`upload.ts` removed. See CLAUDE.md #17. 428 tests pass; build green.
- **2026-06-03** — **Mandatory live-selfie registration + OTP-only (confirmed) ✅** — registration now requires a **live-camera selfie** (`selfie-capture.tsx`: `getUserMedia` → `<canvas>` → JPEG; **no gallery upload**, image-only, retake-safe) alongside name/phone/BMDC; email stays optional; the old NID/"other docs" pickers are removed. Unauthenticated, IP-rate-limited `uploadRegistrationSelfieAction` streams the blob to the **private** bucket and returns key + sha256/size/mime; `startRegistrationAction` stashes them in `regDraft`; `completeRegistrationAction`/`materializeRegistration` mint the selfie `File` doc (no S3 re-read) and cache `selfieFileId`/`selfieKey`/`selfieBucket` on the `ClaimRequest`. `/admin/verifications` renders the selfie thumbnail via presigned GET. `RegisterSchema` now requires `selfieS3Key` (+ updated validator tests). Login unchanged (already phone-OTP-only; admin keeps email+password). "Always require S3" — no dev escape hatch. 428 tests pass; build green.
- **2026-06-03** — **`npm run lint` clean (0/0) ✅** — cleared 99 errors + 11 warnings of pre-existing debt. The deliberate `Model<unknown>` `{ method: Function }` casts (51) → a shared **type-only** `Loose` interface (`src/lib/db/models/loose.ts`), behavior-neutral; `no-explicit-any` relaxed for `tests/**` + `scripts/**` and `no-unused-vars` set to ignore `^_` (eslint.config.mjs); pagination's inner `Item` hoisted to module scope; leaflet ref-write moved into an effect; `<a>`→`<Link>`, unused imports removed, `prefer-const`/unescaped-entity fixed; justified `eslint-disable` for rules misfiring on correct code (Satori/QR `<img>`, `@react-pdf/renderer` `<Image>` alt, `Date.now()` in async Server Components, react-hook-form `watch`, close-menu-on-route-change). Full gate green: lint + typecheck + 428 tests + build.

- **2026-05-30** — **Sprint B — Unified directory + Loop A status fields + Loop D viral kit ✅** — Full multi-source ingest pipeline (popular-diagnostic 3,237 + ibn-sina 1,932 + sasthyaseba 2,332 + doctor-bangladesh 7,206) merges into 15,879 canonical Doctor rows in ~50s via `npm run ingest`. Provider normalizers under `scripts/lib/providers/` (popular refactored to share normalize layer, three new). Shared normalize layer at `scripts/lib/normalize/` (phone, name, schedule, specialty + 80 aliases, address with BD admin hierarchy). Merge engine at `scripts/lib/merge/` — phone/BMDC hard dedup with switchboard-phone ban (3+ uses), name+chamber+specialty/district triangulation, source-tier-ordered scalar selection with low-confidence fallback, array union with stable per-field keys, JSON audit log per run. Slug collisions disambiguated globally before any DB write. Bulk upsert via Mongoose `bulkWrite` (500/batch, ~25× faster than serial). Existing-row lookup matches by `(slug)` OR `(sourceProvider, sourceProviderId)` so multi-source slug shifts don't miss. Claimed rows protected: only empty fields are filled. **Loop A** — `Doctor` extended with `designation, institute, yearsOfExperience, awards[], memberships[], publications[], concentrations[], metrics{}, socialLinks.youtube`; all pure-additive, FHIR-mapped under `https://daktar.link/fhir/` (nested extensions for arrays). Public profile renders the new chips + new `ProfileCredentials` card. View-counter chip shows `metrics.profileViews30d` when ≥100. Dashboard `StatusEditor` and `CredentialsEditor`, single-CTA completeness nudge that picks the highest-weight missing section. **Loop D** — shared `src/lib/share/text.ts` builder; `ShareToWhatsappButton` opens `wa.me/?text=…`; new `/api/og/[slug]/square` 1080×1080 PNG; new `/api/qr-card/[slug]` 1050×600 print-ready visiting-card. Dashboard "Share your profile" card surfaces both download links to published doctors. 295/295 tests, production build green (33 routes). Deferred: socials + concentrations dashboard editors, mentor/institute graph (Loop B), live serial board + fees rendering (Loop C).
- **2026-06-04** — **SSL Wireless iSMS Plus v3 — SMS provider facade ✅** — `sendSms`/`sendSmsBatch` ([src/lib/sms/client.ts](../../src/lib/sms/client.ts)) are now a **stable facade** delegating to the provider chosen by `SMS_PROVIDER` (default `ssl`; `mdl` retained as a one-env rollback) via `provider.ts` + `providers/{ssl,mdl}.ts` + `types.ts` + `estimate.ts`. SSL path: JSON POST to `https://smsplus.sslwireless.com/api/v3` (`api_token`+`sid`; **request IP must be whitelisted** or every call returns `FAILED`). `sendSmsBatch` partitions a cohort into `/send-sms/bulk` (≤100 same-body) + `/send-sms/dynamic` (≤100 personalized) — a strict win over MDL's 1-per-call; per-recipient status from `smsinfo[]`, `reference_id`→`messageId`, `csms_id`/`batch_csms_id` ≤20. New env `SSL_SMS_API_TOKEN`/`SSL_SMS_SID`/`SSL_SMS_API_BASE_URL`/`SMS_PROVIDER`. Failed sends now log (`[ssl SMS] … failed: …`) instead of being swallowed. Verified live against SSL (single send `status:"SUCCESS"`). CLAUDE.md #14 + deferrals (`/secure/otp-sms` HMAC+AES hardening; SSL exposes no delivery webhook). 446 tests pass; build green.
- **2026-06-04** — **Login UX: clear "no account" message ✅** — `requestLoginOtpAction` now returns "No account found with this number. Please register first." for unknown phones — the silent enumeration-protection no-op was removed (UX over enumeration resistance; the per-phone rate limiter still applies). Both login + registration surface a real error when a configured SMS provider fails to send (e.g. IP not whitelisted), instead of advancing to the OTP screen; the dev no-op still "succeeds" so offline testing works.
- **2026-06-04** — **Contact private by default + opt-in WhatsApp appointment ✅** — `privacyHidePhone`/`privacyHideEmail` now **default `true`**; new `whatsappAppointmentEnabled` (default `false`) gates the "Chat on WhatsApp" appointment button. Enforced at the profile page, the **FHIR mapper** (so `/api/v1/doctors[/slug]` telecom is filtered — no leak), and the dashboard + admin contact forms; the WhatsApp button never falls back to a hidden phone. Pre-production: dropped + reseeded so new docs pick up the hidden-by-default schema (no migration script). Chamber/facility phones stay public (clinic lines, not personal). CLAUDE.md #18. 454 tests pass.
- **2026-06-04** — **Chamber `city` → `district` rename + division/district dropdowns ✅** — `Doctor.chambers[].city` renamed to `.district` across the model/index/types/validator/queries/components/API/FHIR-source/SEO/sitemap/ingestion, aligning the embedded subdoc with the `Chamber` catalog (already `district`). **Division + district are now cascading dropdowns** in the shared dashboard/admin chamber editor, sourced from new [`src/lib/geo/bd-districts.ts`](../../src/lib/geo/bd-districts.ts) (8 divisions, 64 districts; division filters the district list; legacy values tolerated). Route folder `/[slug]/[city]` → `/[slug]/[district]` (URL value unchanged — SEO-safe). Public API accepts `?district=` with a deprecated `?city=` alias; **FHIR output unchanged** (`address.city` still emitted, sourced from district). Pre-production: drop + reseed adopts the rename — the ingestion writers emit `district` directly (no migration script). CLAUDE.md #19. 454 tests pass; build green.
- **2026-06-05** — **Two separate verification flows: BMDC form-hiding fix + Account (identity) verification + name binding ✅** — (1) **Bugfix**: `/dashboard/verification` now gates each form on the doctor's own state, not on a stray pending `ClaimRequest` — a BMDC-verified doctor sees "BMDC verified ✓" (no form). The page is now three cards: status + a **BMDC** card + an **Account** card, each collapsing to a verified/under-review state once resolved. (2) **New account/identity verification**: new `IdentityVerificationRequest` model (mirrors ClaimRequest SLA hooks/indexes, separate so the BMDC login-approval gate stays untouched), new `doctor_identity` private S3 upload purpose + `uploadIdentityDocAction`, new `account-form.tsx` (legal first/last + ID type NID/Passport/Driving-License + Gov-ID photo), `requestAccountVerificationAction`. New admin queue at **`/admin/account-verifications`** (`listPendingIdentityRequests` + SLA buckets + `IdentityReviewRow` with presigned-GET ID preview; "Approve & grant verification" — no login unlock), sidebar gains an **"Account ID"** item (existing one relabeled **"BMDC queue"**), count threaded layout→shell→sidebar. (3) **Blue tick is combined**: `verificationLevel` enum gains `identity_verified`; shared pure `computeVerificationLevel(bmdc, nid)` ⇒ `fully_verified` only when **both** — `VerifiedBadge` renders `fully_verified` as the blue "Verified" tick (BMDC-only / identity-only show their own chip). Reuses the existing `nidVerified` flag; `approveClaimAction` refactored onto the helper. (4) **Name binding**: approval sets the profile name to the verified NID legal name, locks the public display name to `prefix + first + last`, and snapshots `legalName{first,last}`; a shared pure `resolveVerifiedNameUpdate` wired into **both** `updateProfileBasicAction` (doctor) and `adminUpdateProfileBasicAction` (admin) **revokes** identity verification if first/last later change (drops `fully_verified`→`bmdc_verified`, `identity_verified`→`unverified`); a prefix-only change keeps the badge. New `nidVerified` FHIR extension. New Doctor fields (`nidVerifiedAt`, `legalName`, `idDocumentType`) default null/false — no migration. Verified live: public-profile badge SSR, admin login + new queue page + sidebar nav. CLAUDE.md #20. 467 tests pass (+13); typecheck + lint + build green.
- **2026-06-05** — **Public badge explainer popover ✅** — clicking the verification badge on a public profile now opens a popover explaining what it means for *that* profile. New client component [`verified-badge-explainer.tsx`](../../src/components/profile/verified-badge-explainer.tsx) (local-state popover + click-outside catcher + Escape close — no new dependency) wraps the static `VerifiedBadge` and renders a plain-language title + a **two-axis breakdown** (BMDC registration ✓/✗ + government-ID identity ✓/✗) + a footer noting the blue tick needs both. Wired into [`profile-header.tsx`](../../src/components/profile/profile-header.tsx) **only**; the badge stays static everywhere else (admin list, search cards, home, dashboard), so just the public surface pays for the client island. `role="dialog"` + descriptive trigger `aria-label`; tap / click-outside / Escape all close. Doc-only `verificationLevel`-driven copy, exhaustive by typecheck. No new tests (verified live in preview); typecheck + lint + build green.
- **2026-06-05** — **Admin verification override (edit page) ✅** — ops can now set BMDC + account/identity verification **directly** from `/admin/doctors/[slug]/edit` (new Verification card → [`adminUpdateVerificationAction`](../../src/server/actions/admin-doctor.ts)), no request/queue — for profiles created on a doctor's behalf. Sets `bmdcNumber` + `bmdcVerified`/`nidVerified`, recomputes `verificationLevel` (the blue tick needs both); **granting account/identity requires uploading a Gov photo ID** (`adminUploadIdentityDocAction` → private bucket → stored on new `Doctor.identityDocumentFileId`, shown back on the edit page as a presigned "view" link), then snapshots the *current* profile name as the `legalName` binding and locks the display name to "prefix first last" (#20), so a later first/last edit revokes it via the existing `resolveVerifiedNameUpdate` guard. (`approveAccountVerificationAction` stamps the same field from the request doc, so the verified ID is retrievable regardless of path.)
- **2026-06-05** — **Approval gate moved: login open, publishing gated ✅** — `User.approved` no longer blocks sign-in; it now gates **publishing + public visibility** only. New doctors **auto-sign-in** right after registration (the register form reuses the just-verified OTP via `signIn("sms-otp")`; `completeRegistrationAction` leaves the OTP valid instead of clearing it, and the old "pending — you can't sign in yet" splash is gone) → land on `/dashboard?welcome=1`. Both login blocks removed (`requestLoginOtpAction` + the `sms-otp` `authorize` no longer check `approved`). [`setPublishStatusAction`](../../src/server/actions/doctor.ts) refuses to publish while `approved === false`; the dashboard [`PublishToggle`](../../src/app/(dashboard)/dashboard/profile/publish-toggle.tsx) disables Publish with an explainer, and the overview shows an **"Account under review"** banner. New owner-only **`/preview`** route ([(public)/preview](../../src/app/(public)/preview/page.tsx), auth-gated) renders the profile through a new shared [`DoctorProfileView`](../../src/components/profile/doctor-profile-view.tsx) extracted from `/[slug]` (preview mode hides the claim banner / appointment dialog / report button); the dashboard "Preview" link now points there (it used to 404 on drafts). Public `/[slug]` + search + sitemap + OG + API stay gated on `status === "published"` (no per-render `approved` lookup — publish-gating already implies approval, so the SEO hot path stays fast). Admin **"Approve & unlock login" → "Approve & allow publishing"**; queue badge "login blocked" → "can't publish yet"; `approveClaimAction` still flips `approved → true` (now unlocks publishing, doesn't auto-publish). CLAUDE.md #1b/#1c rewritten. No schema changes / no migration. 467 tests pass; typecheck + lint + build green.
- **2026-06-05** — **Publish requires a minimum-complete profile (4 mandatory fields) ✅** — even after admin approval, a doctor can't publish until the profile carries **name & display title, profile photo, ≥1 specialty, ≥1 qualification**. New pure helper [`missingPublishRequirements`](../../src/lib/utils/completeness.ts) + `MANDATORY_PUBLISH_KEYS` filter the existing `computeCompleteness` sections (keys `basic`/`photo`/`specialties`/`qualifications`) — no new fields/derivation. [`setPublishStatusAction`](../../src/server/actions/doctor.ts) blocks publish (after the approval gate) with the missing labels; the dashboard [`PublishToggle`](../../src/app/(dashboard)/dashboard/profile/publish-toggle.tsx) gains `missing` + `blockOnMissing` props (disables Publish + lists what's needed for doctors); the overview "Finish your profile" checklist tags those 4 "required to publish". **Admins keep an override** — `adminSetPublishStatusAction` stays ungated, the admin editor's publish card warns instead of blocking (`blockOnMissing={false}`), and the audit log records `incompleteOverride`. Gate fires only on the `draft → published` action (already-published profiles aren't retroactively unpublished). CLAUDE.md #1c. New [tests/publish-requirements.test.ts](../../tests/publish-requirements.test.ts) (+ fixed the `admin-doctor-actions` completeness mock). 499 tests pass; typecheck + lint + build green. Validates BMDC format (required to mark BMDC verified) and catches the unique-index collision (11000 → friendly error). Deliberately does **not** touch `User.approved` (login stays gated by the BMDC claim queue). New client section [`sections/verification-section.tsx`](../../src/app/admin/doctors/[slug]/edit/sections/verification-section.tsx) with a live "Resulting badge" preview. Verified live in preview: setting both flips the public profile to the blue **Verified** tick (revalidate busts the ISR cache); un-verify reverts. CLAUDE.md #20. 467 tests pass; typecheck + lint + build green.
- **2026-06-06** — **Server-side image optimization on upload + `next/image` delivery tuning + blur-up ✅** — every uploaded image is now resized + recompressed **before** it streams to S3, via new [`optimizeImageBuffer`](../../src/lib/images/optimize.ts) (`sharp`, **format-preserving** — `next/image` negotiates AVIF/WebP at *display* time, so no transcode/key/MIME churn at upload). Wired into all three convergence points: profile/cover ([doctor-photo.ts](../../src/lib/s3/doctor-photo.ts), 1024/1600px @ q80), Gov-ID/BMDC docs ([upload-doc.ts](../../src/lib/s3/upload-doc.ts), 2400px @ q85 — **PDFs pass through untouched**), registration selfie ([photo.ts](../../src/server/actions/photo.ts), 1280px @ q82); admin photo/identity uploads inherit it via the shared helpers. A multi-MB phone photo typically drops ~80–90%. Sequence is `optimize → computeSha256 → uploadBufferToS3` so `File.sizeBytes`/`sha256`/the stored bytes all reflect the compressed buffer. `.rotate()` bakes EXIF orientation + strips metadata (incl. GPS); a pixel-ceiling **decompression-bomb guard** rejects oversized/corrupt input (matters most on the unauth selfie path), while a recompress failure *after* a clean decode falls back to the original buffer (never blocks a valid upload). **Delivery** ([next.config.ts](../../next.config.ts) `images`): `formats:['image/avif','image/webp']` + `minimumCacheTTL` 1yr (keys are timestamped + immutable) + exact `imageSizes` for the avatar boxes — **verified live** (optimizer serves `Content-Type: image/avif` at the precise `w=72`). **Blur-up**: `generateBlurDataUrl` cached on new `Doctor.photo.blurDataUrl` (denormalized PhotoSchema, #12); [profile-header.tsx](../../src/components/profile/profile-header.tsx) + [doctor-card.tsx](../../src/components/search/doctor-card.tsx) render `placeholder="blur"` (existing rows are `null` → no fade, graceful, no migration). `sharp@0.34.5` added as a **pinned exact** direct dep (matches Next's bundled copy → npm dedupes to one install; **not** added to `serverExternalPackages`; confirmed traced into `.next/standalone/node_modules/@img/`). New [tests/image-optimize.test.ts](../../tests/image-optimize.test.ts) (8 cases: dimension cap, format preserved, EXIF auto-orient, PDF passthrough, corrupt-input reject, pixel-cap reject, blur data URI). CLAUDE.md #22. Deferred: **CloudFront** in front of `/_next/image` (durable optimizer-cost fix — Fargate's cache is ephemeral + per-task, AVIF is CPU-heavy to encode); a re-optimize backfill for pre-#22 photos. 542 tests pass (+8); typecheck + lint + build green.
- **2026-06-06** — **Deployment retargeted to AWS EC2 (PM2 + nginx) ✅** — deploy target changed from ECS Fargate behind ALB to a single **EC2** instance running `next start` under **PM2** behind **nginx** (TLS + reverse proxy). New [`ecosystem.config.cjs`](../../ecosystem.config.cjs) runs `next start -H 127.0.0.1 -p 3000` — the localhost bind is **security-critical** (nginx forwards the trusted `X-Real-IP`; a directly-reachable port would let a client forge it and bypass the per-IP limiters, #21), and `next start` ignores a `HOSTNAME` env var so the host must be passed via `-H` (the standalone Docker `server.js` is the opposite — it reads `HOSTNAME`). Secrets load from gitignored `.env.production.local` (Next's dotenv). **Redis: no code change** — kept Upstash over its **REST** API (`@upstash/redis` + `@upstash/ratelimit`). REST runs fine on a long-running server, and `@upstash/ratelimit` v2 *requires* the REST client (its `redis` param is typed against `@upstash/redis`; `ioredis`/TCP is incompatible) — switching to TCP would mean ripping out the 13 sliding-window limiters and hand-rewriting them in Lua for no functional gain on EC2. Docs retargeted: CLAUDE.md (deploy-target + Shared-state rows, #17 instance-profile creds, #22 sharp-binary/optimizer-cache notes), README deploy section (EC2/PM2/nginx walkthrough), getting-started §11.5 (PM2 + the `-H` gotcha), `.env.example`, and the `next.config.ts` standalone comment. `output: 'standalone'` + the multi-stage Dockerfile retained as an alternative container path. Docs/config only — no app code touched; lint + typecheck + build green.
- **2026-06-16** — **DocTime ingestion — 1,381 new doctors ✅** — added DocTime (telemedicine; 1,558 scraped rows) as a fourth ingest source and merged it **incrementally** against the existing unified corpus so no already-seeded `unifiedId` changes. New normalizer [`scripts/lib/providers/doctime.ts`](../../scripts/lib/providers/doctime.ts) — `reg_no`→BMDC on `dedupKeys.bmdc`; **chamberless** (~99% of DocTime is virtual-only with no phone and no district, and the 21 one-off clinic addresses aren't a reusable branch network, so they're dropped); `"doctime"` added to the source union + the `build-normalized` driver. New [`scripts/build-doctime-incremental.ts`](../../scripts/build-doctime-incremental.ts) reuses the **exact** `sameDoctor`/`connectedComponents`/`foldCluster` engine from `build-unified.ts`: blocks by nameKey, de-dups DocTime-internally, then classifies each cluster against frozen production nameKeys — **NEW** (no name match) → emit; **REVIEW** (name collides; cross-source has no phone/district so the rules can't auto-confirm — exactly the existing review-queue path) → [`data/unified/doctime-review-queue.md`](../../data/unified/doctime-review-queue.md), never auto-inserted. Then a global **BMDC de-dup** across NEW clusters (DocTime is the only BMDC-bearing source, so it never collides with production). Result: **1,381 new doctors** (12 dropped as BMDC dupes; **0 `unifiedId` collisions** with the 4,899 frozen docs) + **152 records / 123 name-groups → review queue**. `foldCluster` now carries `bmdcNumber` (first non-empty `dedupKeys.bmdc`; popular/ibn supply none → existing output + ids unchanged). `seed-unified.ts` gains `--file=` (seed only the new docs) and writes the scraped BMDC to **`Doctor.bmdcNumber` + `registrations[]`** (per decision — accepts that a future registration whose BMDC matches a scraped row can hit the unique index; the seeder now tolerates the 11000 and skips rather than aborting the batch). Seeded into the dev DB: **+1,381 published, unclaimed, telemedicine (0 chambers), 1,337 with BMDC; 0 dup-key skips** (4,902 → 6,283 docs). **Bonus resolver bugfix**: "Dentistry" → **"Dental Surgery"** (was matching the 3-char canonical "ENT" inside "d-**ent**-istry"); added the missing `dentistry` alias + a word-boundary guard so short canonicals can't substring-match inside longer words — fixes 98 dentists + 2 "Complementary & Alternative Medicine" rows. New [tests/providers/doctime.test.ts](../../tests/providers/doctime.test.ts). 563 tests pass (+21); typecheck + lint (0/0) + build green. Deferred: DocTime **photos** (wire `--file=` into `seed-unified-photos.ts` + run with S3 creds) and a human pass over the 152-record review queue.
- **2026-06-17** — **Registration no longer wedges on a BMDC collision (+ auto-attach by BMDC) ✅** — fixed a bug where registering with a BMDC already held by an **unclaimed** directory profile (e.g. one of the 1,337 scraped DocTime rows) threw a raw `E11000` from the unique `bmdcNumber` index *after* OTP verification, leaving a verified User with **no Doctor** ("No profile yet"), unable to re-register (phone taken) and unable to recover. The app-level BMDC guard only checks `isClaimed:true` ([auth.ts:143](../../src/server/actions/auth.ts)), so an unclaimed collision sailed past it straight into the DB unique index ([Doctor.ts](../../src/lib/db/models/Doctor.ts), `bmdc_unique_when_present_idx`). Three changes in [auth.ts](../../src/server/actions/auth.ts): **(1) claim-or-create** in `materializeRegistration` — a BMDC matching an unclaimed profile now **auto-attaches** the registrant to it (atomic `findOneAndUpdate({bmdcNumber, isClaimed:false})` → owner + pending `ClaimRequest`/selfie, same admin gate as a slug-claim) instead of `Doctor.create`; an `E11000` backstop catches a create race and either claims the raced doc or returns a friendly, recoverable error (never a 500). **(2) Re-registration unblock** — the phone-clash guard now blocks only a verified phone that already has an *actual provisioned Doctor*, so a verified-but-profileless User (a prior failed registration) can re-register and recover. **(3)** the dashboard [`No profile yet`](../../src/app/(dashboard)/dashboard/page.tsx) card is now actionable (a "Finish registration" link → `/auth/register`) instead of dead-end "sign out and register again" text. Verified end-to-end against the dev DB: BMDC-match → attaches to the existing profile (1 doc, claimed, owned by registrant, 1 ClaimRequest, **no E11000**); no-match → fresh profile. 563 tests pass; typecheck + lint + build green. (The 1 already-wedged account recovers by re-registering once this ships.)
- **2026-06-17** — **DocTime BMDC normalized to digits-only ✅** — the scraped `reg_no` was stored in mixed shapes (`A-7676`, `A 7383`, `A7676`, `9908BDS`, plus junk `N/A`/`0`/`88`). The DocTime provider ([doctime.ts](../../scripts/lib/providers/doctime.ts)) now strips every non-digit and keeps the value only when it's a valid 4–7 digit BMDC (via `isValidBmdcFormat`) → one canonical numeric format; junk/implausible values become "no BMDC". **Scoped to DocTime ingestion** — the shared `normalizeBmdc` (and the dental `A-` prefix for *live* registrations) is untouched. Regenerated `data/unified/doctime-new.json` + re-seeded + nulled the leftover junk in the DB. **Bonus fix:** stripping revealed the old run was **wrongly de-duplicating ~12 distinct doctors** who shared a junk `reg_no` (14 × "Not Applicable" all collapsed to one BMDC string) — they're now correctly retained, so the cohort is **1,393** (was 1,381), with **1,331 distinct digit BMDCs, 0 invalid** in the DB. Strengthened [tests/providers/doctime.test.ts](../../tests/providers/doctime.test.ts) with a digits-only regression guard. 563 tests pass; typecheck + lint green.
- **2026-06-24** — **Outbound conversion campaign: email channel + personalized claim deep-links ✅** — extended the A.8 outbound system to drive **profile claims** over **SMS + email**. **(1) Fixed a dead claim link** — the broadcast SMS templates linked to `https://daktar.link/claim` (a 404); the claim templates ([templates.ts](../../src/lib/outbound/templates.ts)) are now **personalized** and deep-link to each doctor's own `https://daktar.link/auth/register?slug={{slug}}` (the working claim entry that pre-confirms the profile), which also fixes claim attribution. **(2) Built the email channel** — new bulk facade [`sendEmailBatch`](../../src/lib/email/client.ts) (concurrency-bounded fan-out over the 1-per-call SESv2 `sendEmail`; per-recipient `SuppressedRecipientError` → `suppressed` row + batch continues; dev no-op → all sent; coarse `globalEmailBudgetLimiter`), a parallel campaign-template registry [`email-templates.ts`](../../src/lib/outbound/email-templates.ts) (`OUTBOUND_EMAIL_TEMPLATES` + `renderEmailTemplate` — subject + HTML personalized with `claimUrl`/`unsubscribeUrl`, reuses the branded `shell()` + `escapeHtml`), and a public one-click unsubscribe route [`/api/unsubscribe`](../../src/app/api/unsubscribe/route.ts) backed by HMAC-signed tokens ([unsubscribe-token.ts](../../src/lib/outbound/unsubscribe-token.ts), `UNSUBSCRIBE_SECRET`→`AUTH_SECRET` fallback) + an IP limiter. **(3) Channel-aware infra** — `OutboundMessage.channel` gains `"email"` + a `"suppressed"` status; `OptOut` generalized to `{ channel, phone?, email? }` with partial-unique indexes per field (#5a); [`scripts/outbound.ts`](../../scripts/outbound.ts) gains `--channel=sms|email` via a small `ChannelStrategy` (shared cohort/idempotency/telemetry; channel-specific contact field, normalizer, opt-out lookup, dispatch); `completeRegistrationAction` attribution now matches phone **and** the registrant's emails so email-driven claims register; `/admin/outbound` shows Channel + Suppressed columns and a channel-aware opt-out roster/form. **Data reality:** all 3,237 seeded doctors have a phone (SMS = the lever); only ~41 have an email today (channel built for the growing base + free per-send). New helper [`normalizeEmail`](../../src/lib/utils/email.ts). **Verified end-to-end** against the dev DB: SMS dry-run renders the `/auth/register?slug=` deep-link (no `/claim`); email dry-run matches exactly 41 reachable with a personalized subject + no unresolved placeholders; `/api/unsubscribe` returns the right page for valid/invalid/missing tokens and writes the correct `OptOut{channel:"email"}` row. 4 new test files (email-batch, email-template, unsubscribe-token, email-normalize); **650 tests pass**; typecheck + lint (0/0) + build green (`/api/unsubscribe` registered). New env: `EMAIL_GLOBAL_HOURLY_CAP`, `UNSUBSCRIBE_SECRET`. **Operational prerequisites for live sends** (not code): **SES production access + verified `From` domain** for email (sandbox only reaches verified recipients); **SSL Wireless IP whitelist** for SMS. Deferred: Bangla email variant, plain-text email part + `List-Unsubscribe` header, mirroring email opt-outs into the SES DynamoDB suppression list.
- **2026-06-24** — **Claim email rebuilt to mirror the public profile page ✅** — replaced the simple `{{var}}` mustache template with a typed [`render(ctx)`](../../src/lib/outbound/email-templates.ts) function emitting a single-column, email-safe rendering of the `/[slug]` profile page itself: gray page, Daktar.Link header, white bordered "cards" (header with avatar + Unverified/Unclaimed chips + meta line · the amber "Are you Dr. X? — Claim this profile · Free · phone + SMS verification" banner from the public page · About · Qualifications · Experience · Chambers & schedule including a `<table>`-based Sat→Fri grid with teal-on-open / muted-on-closed cells · Areas of focus pills · Languages pills) and a centered footer with the one-click unsubscribe link. All table-based, inline-styled, with a bulletproof `<td bgcolor>` button — no flexbox/grid/SVG — so it renders identically in Gmail, Outlook, and Apple Mail. The About paragraph reuses the page's own [`buildAutoProfileSummary`](../../src/lib/seo/profile-text.ts) so it's deterministic, unique per doctor, and matches the on-page text. [scripts/outbound.ts](../../scripts/outbound.ts) now projects the richer field set (qualifications, experience, chambers + schedule, languages, sub-specialties/concentrations, designation, institute, BMDC, views) **only on the email channel** (`ChannelStrategy.selectFields`), so SMS still loads the lean projection. Every doctor-derived value is HTML-escaped; optional cards (chamber, qualifications, experience, focus, languages) self-omit when empty so partial profiles still send a clean email. **Verified**: 653 tests pass (7 in [tests/outbound-email-template.test.ts](../../tests/outbound-email-template.test.ts) covering section rendering, deep-link/no-`/claim`, escaping, optional-section omission, no leftover mustache); typecheck + lint (0/0) + build green; real-template render against a sample doctor produces 10.8 KB of HTML; email dry-run against the dev DB still wires correctly (cohort + personalized subject). Deferred (unchanged): Bangla email variant, plain-text part + `List-Unsubscribe` header.
